        </div> <!--main-->

    </div>
        <nav class="navbar navbar-default navbar-static-bottom"><!--navbar-static-bottom并不在bootstrap里面存在，但我在style.css里面写了相关样式-->
            <div class="container-fluid">
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav col-md-12">
                        <li class="col-md-12"><p>
                            Copyright © 2015-2016 : 上海万达信息股份有限公司, All Rights Reserved
                        </p></li>
                    </ul>
                </div>
            </div>
        </nav>


    <!--轮播，JS启动方式-->
    <script>
        //方法1，将带有"carousel"类样式的元素作为轮播组件启动
        $('.carousel').carousel()
        //方法2，id="myCarousel"的元素作为轮播组件启动
        //$('#myCarousel').carousel()
        //$('#myCarousel').on('slid', function () {
        //alert("dfdf");
        //})
    </script>
<!-- Javascript - jQuery -->
<script src="http://code.jquery.com/jquery.min.js"></script>
<script>window.jQuery || document.write('<script src="<%=static_dir%>/js/jquery-1.7.2.min.js"><\/script>')</script>
<!--[if (gte IE 6)&(lte IE 8)]>
<script src="<%=static_dir%>/js/selectivizr.js"></script>
<![endif]-->
<script src="<%=static_dir%>/js/jquery.flexslider-min.js"></script>
<script src="<%=static_dir%>/js/scripts.js"></script>
        <script type="text/javascript">
            // 设置为主页
            function SetHome(obj,vrl){
                try{
                    obj.style.behavior='url(#default#homepage)';obj.setHomePage(vrl);
                }
                catch(e){
                    if(window.netscape) {
                        try {
                            netscape.security.PrivilegeManager.enablePrivilege("UniversalXPConnect");
                        }
                        catch (e) {
                            alert("此操作被浏览器拒绝！\n请在浏览器地址栏输入“about:config”并回车\n然后将 [signed.applets.codebase_principal_support]的值设置为'true',双击即可。");
                        }
                        var prefs = Components.classes['@mozilla.org/preferences-service;1'].getService(Components.interfaces.nsIPrefBranch);
                        prefs.setCharPref('browser.startup.homepage',vrl);
                    }else{
                        alert("您的浏览器不支持，请手动添加："+vrl);
                    }
                }
            }
            // 加入收藏 兼容360和IE6
            function setFavor(sTitle,sURL)
            {
                try
                {
                    window.external.addFavorite(sURL, sTitle);
                }
                catch (e)
                {
                    try
                    {
                        window.sidebar.addPanel(sTitle, sURL, "");
                    }
                    catch (e)
                    {
                        alert("加入收藏失败，请使用Ctrl+D进行添加："+sURL);
                    }
                }
            }
        </script>
        </body>
</html>